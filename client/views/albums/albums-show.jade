.row
  .col-sm-4.col-sm-offset-4
    h2 {{name}}
.row
  .col-sm-2.col-sm-offset-4
    img.img-responsive.img-thumbnail.feature(ng-src='{{afPhotos[0].$value}}')
    button.btn.btn-warning(ng-click='deleteImg(photo, $index)') Delete Image
    
  .col-sm-4
    h3 Contact Information
    h4 Address: {{printAlbum.description}}
    h4 Birthday: {{printAlbum.date | date: 'MM-dd-yyyy'}}
    h4 Telephone: {{printAlbum.phone}}
    h4 Email: {{printAlbum.email}}
    h4 Homepage: {{printAlbum.url}}
    h4 Facebook: {{printAlbum.facebook}}
    h4 Twitter: {{printAlbum.twitter}}

.row
  .col-sm-4.col-sm-offset-4
    button.btn.btn-warning(ng-click='goBack()') Back
.row
  .col-sm-4.col-sm-offset-4
    
    
    form
      .form-group
        
        label Address
        input.form-control(type='text', ng-model='album.description')
        label D.O.B.
        input.form-control(type='date', ng-model='album.date')
        label Telephone
        input.form-control(type='tel', ng-model='album.phone')
        label Email
        input.form-control(type='text', ng-model='album.email')
        label Homepage
        input.form-control(type='text', ng-model='album.url')
        label Facebook
        input.form-control(type='text', ng-model='album.facebook')
        label Twitter
        input.form-control(type='text', ng-model='album.twitter')
      .form-group
        button.btn.btn-success.center-block(ng-click='saveAlbumInfo(album)') Update Contact Info
    form
      .form-group
        label Add Photo
        input.form-control(type='file', ng-model='photo')
      .form-group
        button.btn.btn-success.center-block(ng-click='convertPhoto(photo)') Add Photo
      .form-group
        img.previmg.center-block

    
    //
//- .row
//-   .col-sm-2.col-sm-offset-5
//-     table.table.table-striped
//-       thead
//-         tr
//-           th Image
//-           th Delete
//-       tbody
//-         tr(ng-repeat='photo in afPhotos track by $index')
//-           td: img.img-responsive.img-thumbnail.gallery(ng-src='{{photo.$value}}')
//-           td: button.btn.btn-danger(ng-click='deleteImg(photo, $index)') Delete
    
